Important implementation rules:
- Do not rewrite unrelated modules
- Do not rename public APIs unless necessary
- Prefer additive refactor over destructive rewrite
- Preserve existing behavior unless this phase explicitly changes it
- Keep diffs reviewable
- Add comments only where they improve maintainability
- If a risky migration is needed, add an adapter layer instead of replacing everything at once
- Never collapse business facts into prompt-only logic
- Keep state, decision, payload, and outcome contracts explicit

You are implementing Phase 7 of a chat-agent refactor.

## Objective
Fix the media inbound path so logging matches actual behavior.

## Problem
The current media path logs "auto-reply queued" but the audit found no actual reply dispatch in that path. This creates observability/runtime mismatch and possible user-facing silent failures.

## Scope
Investigate and implement one of the following:
1. correctly dispatch the intended media auto-reply, or
2. remove/replace the misleading log if auto-reply is not actually intended

## Required process
- trace the media inbound code path from:
  - ProcessInboundMessageJob
  - media handler
  - orchestrator or reply queue path
  - outbound dispatch service
  - sidecar send path if relevant
- identify whether reply is intended by design
- implement the correct behavior
- ensure final outcome reflects actual behavior

## Constraints
- do not guess intent
- follow the existing product behavior implied by code/tests/comments
- if behavior is unclear, choose the safer path: accurate logging with no false "queued" statement
- integrate with TurnOutcome if available from previous phase

## Tests required
1. media inbound with actual reply dispatch
2. media inbound with intentional no-reply
3. log/outcome consistency
4. no false queued message remains

## Deliverables
1. code fix
2. tests
3. markdown explanation of chosen behavior and why

## Definition of Done
- no mismatch remains between media-path log and actual runtime behavior
- operator can trust logs again